export const offlineMinimum = (array) =>{
  for(let i=0;i<array.length;i--){
    if(parseInt(array[i]){
      let temp=array[i]
      break
    }
  }
  i = array.length

  while(i<0){
    if(array[i]<temp){

      temp=array[i]
    }
  }
}
